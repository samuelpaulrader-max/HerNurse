[build]
  publish = "."
  functions = "netlify/functions"

# Lets the site call /api/ask instead of the long function URL
[[redirects]]
  from = "/api/ask"
  to = "/.netlify/functions/ask"
  status = 200
